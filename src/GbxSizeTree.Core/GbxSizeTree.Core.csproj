<Project Sdk="Microsoft.NET.Sdk">

  <!-- Core analysis library. MUST NOT reference Spectre.Console or any rendering/UI package:
       the Core/Cli seam is what keeps `-\-json` output and golden tests honest. -->

  <ItemGroup>
    <!-- GBX.NET pinned EXACTLY: CumulativeChunkMeasurer mirrors CMwNod.Write internals. -->
    <PackageReference Include="GBX.NET" Version="[2.4.4]" />
    <PackageReference Include="GBX.NET.LZO" Version="[2.1.6]" />
    <!-- The compressor behind GBX.NET.LZO.Lzo; referenced directly for the span/pooled overloads. -->
    <PackageReference Include="NativeSharpLzo" Version="[0.3.5]" />
    <PackageReference Include="GBX.NET.ZLib" Version="[1.1.2]" />
    <PackageReference Include="SixLabors.ImageSharp" Version="3.1.12" />
  </ItemGroup>

</Project>
